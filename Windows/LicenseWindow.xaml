<Window x:Class="CN_GreenLumaGUI.Windows.LicenseWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="{DynamicResource Settings_OpenSourceLicenses}" Height="600" Width="800"
        WindowStartupLocation="CenterOwner"
        FontFamily="/Fonts/#阿里巴巴普惠体"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="{DynamicResource License_SpecialThanks}" Style="{StaticResource MaterialDesignHeadline5TextBlock}" Margin="0,0,0,10"/>
            <StackPanel Orientation="Horizontal">
                <materialDesign:PackIcon Kind="Heart" Foreground="Red" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBlock VerticalAlignment="Center">
                    <Hyperlink NavigateUri="https://cs.rin.ru/forum/viewtopic.php?t=103709" RequestNavigate="Hyperlink_RequestNavigate">
                        <TextBlock Text="{DynamicResource License_GreenLumaCredit}" FontSize="16"/>
                    </Hyperlink>
                </TextBlock>
            </StackPanel>
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" MinWidth="180"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <materialDesign:Card Grid.Column="0" Margin="0,0,20,0" VerticalAlignment="Stretch">
                <ListBox x:Name="LicenseListBox" 
                         ItemsSource="{Binding LicenseList}" 
                         DisplayMemberPath="Name" 
                         SelectedIndex="0"
                         SelectionMode="Single"
                         Style="{StaticResource MaterialDesignListBox}"
                         BorderThickness="0">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem" BasedOn="{StaticResource MaterialDesignListBoxItem}">
                            <Setter Property="Padding" Value="16"/>
                            <Setter Property="HorizontalContentAlignment" Value="Left"/>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>
            </materialDesign:Card>

            <materialDesign:Card Grid.Column="1" VerticalAlignment="Stretch" Padding="10">
                <ScrollViewer>
                    <TextBlock Text="{Binding ElementName=LicenseListBox, Path=SelectedItem.Content}" 
                               Margin="10" 
                               TextWrapping="Wrap"
                               FontSize="14"/>
                </ScrollViewer>
            </materialDesign:Card>
        </Grid>
    </Grid>
</Window>
